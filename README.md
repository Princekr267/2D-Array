# 2D Array 🧮

A collection of classic **2D array problems** solved in C++ for learning and practice.

## 📂 Problems Included

### 1. **Diagonal Sum** (`diagnalsum.cpp`)
Calculate the sum of both diagonals in a square matrix.
- **Brute Force:** O(n²) - Traverse entire matrix
- **Optimized:** O(n) - Single loop through diagonals

### 2. **Matrix Search** (`matrix_search.cpp`)
Search for a target element in a sorted 2D matrix.
- **Brute Force:** O(n×m) - Linear search
- **Staircase Search:** O(n+m) - Start from top-right corner

### 3. **Spiral Matrix** (`spiralmatix.cpp`)
Print matrix elements in spiral order (clockwise).
- **Complexity:** O(n×m) - Visit each element once
- Handles edge cases for non-square matrices

### 4. **Matrix Pointers** (`matrix_pointer.cpp`)
Understanding how 2D arrays work with pointers in memory.

## 🚀 How to Run

```bash
# Compile any file
g++ filename.cpp -o output

# Run
./output
```

## 💡 Key Learnings

- Matrix traversal techniques
- Optimization from O(n²) to O(n)
- Pointer arithmetic in 2D arrays
- Boundary handling in algorithms

---

*Happy Coding! 🔥*
